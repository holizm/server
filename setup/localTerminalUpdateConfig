case "$TERM" in
  xterm*|rxvt*|screen*)
    update_title() { printf '\033]0;%s@%s:%s\007' "$USER" "$HOSTNAME" "$PWD"; }
    # append to existing PROMPT_COMMAND safely
    PROMPT_COMMAND="${PROMPT_COMMAND:+$PROMPT_COMMAND; }update_title"
    ;;
esac
