include ${environmentRoot}/${instance}/${process}/nginx/${tenant}/httpsRedirect;

server {
    include ${environmentRoot}/${instance}/${process}/nginx/${tenant}/listen;
    include ${environmentRoot}/${instance}/${process}/nginx/${tenant}/certificate;

    location / {
        proxy_pass http://localhost:$databases;
    }
}
