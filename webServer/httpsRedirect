server {
    listen 80;
    server_name ${subdomain}${domain};

    location ^~ /.well-known/acme-challenge/ {
        root /holism/server/webServer/defaultHtml;
        try_files $uri =404;
    }

    location / {
        return 301 https://$server_name$request_uri;
    }
}
