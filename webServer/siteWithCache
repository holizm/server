include ${home}/${instance}/${process}/nginx/${tenant}/httpsRedirect;
include ${home}/${instance}/${process}/nginx/${tenant}/wwwRedirect;
include ${home}/${instance}/${process}/nginx/${tenant}/cacheConfig;

server
{
    include ${home}/${instance}/${process}/nginx/${tenant}/compression;
    include ${home}/${instance}/${process}/nginx/${tenant}/listen;

    proxy_buffers 4 16k;
    proxy_buffer_size 16k;

    include ${home}/${instance}/${process}/nginx/${tenant}/certificate;

    location /
    {
        proxy_pass http://localhost:${randomPort};
        include ${home}/${instance}/${process}/nginx/${tenant}/proxy;
        include ${home}/${instance}/${process}/nginx/${tenant}/cacheUsage;
    }

    location ~ ^/(${localesRegex})(/|$)
    {
        rewrite ^/(${localesRegex})/(.*)$ /$2?locale=$1 break;
        rewrite ^/(${localesRegex})$ /?locale=$1 break;
        proxy_pass http://localhost:${randomPort};
        include ${home}/${instance}/${process}/nginx/${tenant}/proxy;
        include ${home}/${instance}/${process}/nginx/${tenant}/cacheUsage;
    }
}
