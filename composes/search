services:
    search:
        image: getmeili/meilisearch:latest
        container_name: ${instance}Search
        hostname: ${instance}Search
        ports:
            - 127.0.0.1:${randomPort}:7700
        environment:
            - MEILI_MASTER_KEY=${searchMasterKey}
        volumes:
            - ${environmentRoot}/${instance}/search/databases:/meili_data
        restart: always
