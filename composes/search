services:
    search:
        image: getmeili/meilisearch:latest
        container_name: ${instance}Search
        hostname: ${instance}Search
        ports:
            - 127.0.0.1:${deterministicPort}:7700
        environment:
            - MEILI_MASTER_KEY=${searchMasterKey}
        volumes:
            - ${home}/${instance}/search/databases:/meili_data
        restart: always
