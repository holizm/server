services:
    accounts:
        image: holism/push
        container_name: push
        working_dir: /gesht/push
        ports:
            - 127.0.0.1:${{randomPort}}:3000
        restart: always
        command: node server.js
